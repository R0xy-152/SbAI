# backend Docker build context is the repo root (backend/Dockerfile).
# Keep the image lean and never ship secrets / local runtime data.
backend/.venv/
backend/data/
backend/.pytest_cache/
**/__pycache__/
*.pyc
.git/
docs/
validation-results/
# frontend-deprecated/ 与 char/ backgroud/ 必须留在 build context：
# backend 镜像在容器根静态挂载三棵资源树（docs/13 Task 9 Step 3）。
frontend-vue/
