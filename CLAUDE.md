# CLAUDE.md

兼容入口。仓库唯一权威操作指引是根目录 **`AGENTS.md`**：开始任何任务前必须先读它。
线上运营状态见 `deploy/STATUS.md`。

三条兜底（即使未读 AGENTS.md 也必须遵守）：

1. `/docs` 是真相源：代码与文档冲突以文档为准，先修文档再实现。
2. 执行细节不明确必须向用户提问，不得自做主张。
3. API Key / Secret 只能存在 Backend 环境，绝不进前端、仓库或提交记录。
