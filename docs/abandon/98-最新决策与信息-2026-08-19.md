# 98 — 最新决策与信息（2026-08-19）

> **状态：已归档（2026-08-19）。** docs/13 Task 9（视觉回归与 Vue Cutover）已完成并 PASS，本交接文档随任务结束归档至 `docs/abandon/`。记录见 `validation-results/docs13-task9/result.md`。

> **状态：事实源补充文档（TEMP，随交接使用）。** 本文档记录本次会话的决策与项目最新信息（docs-first 原则下，**以本文 + `docs/99-临时交接文档-2026-08-19.md` 为最新真相源**），并列出与 `D:\gal\docs` 其他文档**相悖或需补充**的全部条目，供后续会话逐项修正。
> 日期：2026-08-19。与 99 号交接文档配套；任务完成后可归档。

---

## 一、本次会话决策（docs/13 Task 9 规划阶段）

以下决策已由用户明确确认，**后续实现必须遵守**：

| # | 决策 |
|---|---|
| D1 | 旧前端 `frontend/` → 移动为 `frontend-deprecated/`（**不删除**，保留参考与回退）。 |
| D2 | 视觉回归用 **Playwright screenshot 基线**（入库；1366x768 + 1920x1080；6 snapshot：TITLE_EMPTY_SAVE / OPENING_DEEPSEEK_ONLY / CLAUDE_APPEARS_TWO_ROLE / LONG_DIALOGUE / SAVE_PANEL / LOAD_PANEL）。 |
| D3 | **「第一章主线可运行」= docs/13 §7.2 验收链路**（Title→New Game→Opening→Player Input→Response→03:17→Claude 出现→Save→Reload→Load→恢复）。 |
| D4 | **线索调查通过调用 LingChat 的「选项功能」实现，这一块先搁置** —— 证据面板 / 推理 / 私审 / 收尾 / 其余 3 个 hotspot 的 Vue UI 推迟到后续任务，Task 9 不实现。 |
| D5 | **Docker Compose 下核心资源 404 不能作为「已知限制」放过** → 必须修复并验证（backend Dockerfile COPY `char/` `backgroud/` `frontend-deprecated/` 进容器根 + nginx.conf 反代，`docker compose up --build` 实测）。 |
| D6 | **03:17 的视觉/E2E 不得用「玩家问 03:17」触发** → 必须走确定性 Gate：获得纸条（EV01）→ `pre_0317_player_turns >= 2` → 自动发生。 |
| D7 | **所有新记录必须写「Vue Cutover」**，不得写「React Cutover」。 |
| D8 | **旧前端是 vanilla HTML/CSS/JS，不是 React**；本项目不存在 React；docs 中所有 "React" 均属文档漂移。 |
| D9 | 本会话暂停 Task 9，交接 + 提交 + push 当前进度。 |

---

## 二、最新权威信息（2026-08-19 快照）

1. **当前前端**：`frontend-vue/`（Vue 3 + Vite + TypeScript + Pinia + TailwindCSS）是唯一现役前端，是默认 dev 入口与默认 Docker 服务。
2. **旧前端**：`frontend/` 是 vanilla HTML/CSS/JS（**非 React、非 Next.js**），已冻结；Task 9 将移至 `frontend-deprecated/`，当前由后端托管于 `/frontend/index.html`。
3. **docs/13 Tasks 0-8 全部完成并 PASS**（0/1 PASS_WITH_LIMITATION，2-8 PASS；Task 5/6/7/8 分别 commit d59591e / 63d5680 / d0cb382 / 71dc034）；**仅 Task 9 剩余且处于规划中**（plan 文件在 `.claude/plans/serene-strolling-rainbow.md`，不在仓库内）。
4. **docker-compose.yml 已定义三服务**：frontend-vue（8080:80）、backend（8000, GAL_PROVIDER=mock, GAL_SAVE_BACKEND=postgres）、postgres（16-alpine + healthcheck）——**已指向 Vue**。
5. **存档系统已完整实现**（Task 6-8）：GameSave / SaveSnapshotService / SaveRepository / Save API；1 AUTO + 6 MANUAL 槽；schema_version；匿名 player_id 命名空间；Load 创建新 Active Session（§19.1）。**但 dev/CI 仍走 JSON 存档仓库**（backend/data）；compose 中的 Postgres 存档接线**尚未成为已验证运行路径**。
6. **Auto Save** 是 Narrative commit 后的 side effect；已实现 5 个 checkpoint（`backend/app/save/checkpoint.py`）：AS_CH1_OPENING_COMPLETE / AS_CH1_CLAUDE_APPEARED / AS_CH1_INF01_CONFIRMED / AS_CH1_INF03_CONFIRMED / AS_CH1_RECOVERY_ENTRY。docs/13 §21.2 中列出的**其余 7 个 checkpoint 未实现**。
7. **03:17 触发是确定性 Gate（A/B）**：EV01 到手 → 置 PRE_0317_WINDOW → 每个 approved 玩家回合 `pre_0317_player_turns += 1` → counter ≥ 2 **或**玩家显式询问 03:17 时触发（`backend/app/script/conditions.py:41`、`backend/app/game/orchestrator.py:823-911`）。**不是系统时间到达 03:17**。
8. **调查主线 UI（Claude 出现之后的证据面板/推理/私审/收尾/其余 hotspot）在 frontend-vue 无实现**，已决定推迟到后续任务用 LingChat 选项功能实现（D4）；后端已充分测试，旧前端有参考 UI。
9. **AGPL**：`THIRD_PARTY_LICENSES.md`（9 项复用表）+ `NOTICE.md` 已存在；**根目录 `LICENSE`（AGPL-3.0 全文）缺失**，Task 9 必须补（Cutover 条件「AGPL notice完成」）。
10. **frontend-vue 无任何测试设施**（无 vitest/playwright），Task 9 需新增 devDependency（§26.1/§26.2 直接理由）。
11. **CLAUDE.md 有用户/编辑器侧在途修改**，一律排除在提交之外；其内部过时表述见下文「三、相悖/补充清单」。
12. **命名规则**：新记录一律「Vue Cutover」/「旧前端 / vanilla 前端」，永不出现 "React Cutover" 或把旧前端标为 React。
13. **验证 fixture 契约**：`GAL_PROVIDER=mock`；shell 带 DEEPSEEK_API_KEY 时必须显式固定 mock，否则走真实 DeepSeek → 测试偶发失败。

---

## 三、与现有文档相悖 / 需补充的清单（42 条，按来源文档分组）

> 分类说明：**contradicts** = 与最新事实相悖（文档错了）；**supplements** = 文档表述不完全/未覆盖最新现状（需要补充）；**stale** = 过时残留（含文档漂移）。

### 3.1 CLAUDE.md（不编辑/不提交，仅记录过时残留）

| 行 | 现状 | 类别 |
|---|---|---|
| :7 | `Task 0-4 已完成…Task 5-9 待执行` → **Task 5-8 已全部 PASS，仅 Task 9 剩余** | stale |
| :53 | 「旧前端 `frontend/`（已冻结，不删、可回退）」→ Task 9 将迁移至 `frontend-deprecated/` | stale |
| :72 | 「compose 仅定义 frontend-vue 服务」→ 三服务已定义（Task 6 完成） | stale |
| :78 | 「PostgreSQL 仍未接入（docs/13 Task 6）」→ postgres/backend 服务已定义（注：Postgres 存档接线尚未成为已验证运行路径） | stale |
| :80 | 「前端双轨：`frontend/` … 与 `frontend-vue/`」→ 双轨将于 Task 9 结束（旧前端迁入 deprecated） | stale |

### 3.2 AGENTS.md（同 CLAUDE.md，仅记录）

| 行 | 现状 | 类别 |
|---|---|---|
| :11 | `Task 5-9 待执行` → **仅 Task 9 剩余** | stale |
| :57 | 旧前端「不删、可回退」→ Task 9 迁移 | stale |
| :76 / :82 | compose 仅 frontend-vue 服务 / PostgreSQL 未接入 → **三服务已定义** | stale |
| :84 | 前端双轨 → Task 9 结束双轨 | stale |

### 3.3 docs/13-LingChat前端源码迁移、开始界面与存档系统落地方案.md

| 行 | 现状 | 类别 |
|---|---|---|
| :17, :18, :112, :385…:2576 | 全篇称「Next.js / React 迁移」「旧 React 前端」「当前 React 版」→ **本项目无 React；旧前端是 vanilla HTML/CSS/JS，当前前端是 Vue 3** | **contradicts** |
| :52 / :61 | 「02 中 Frontend 技术栈 Next.js + React + TypeScript」→ docs/02 §4.1 已在 Task 0 更新为 Vue 3 + Vite + TypeScript + Pinia + TailwindCSS | stale |
| :2055 | Task 9 标题「视觉回归与 **React** Cutover」→ 必须为 **Vue Cutover**（D7） | **contradicts** |
| :2555 / :2509-2548 | §34/§35 把 Task 0 当「首个任务」、把 Task 0→9 当全部待办 → **Tasks 0-8 已完成，仅 Task 9 剩余** | stale |
| :1867-2051 | §25 Tasks 5-8 均以未实现「做什么」编写、无完成标记 → **均已 PASS**（Title Screen / Save Snapshot / Save/Load UI / Auto Save） | stale |
| :2099 / :2504 | 「Docker Compose 默认指向 Vue」列作未来完成项 → **compose 已指向 Vue（三服务）** | stale |
| :2290 | §30 把 frontend-vue/backend/postgres 列作「推荐」布局 → **已在 compose 中实现** | supplements |
| :2082-2094 | Task 9 Cutover 条件未提 Docker 静态资源 404 → **用户决策 D5：必须修复并验证（/char /backgroud /frontend-deprecated）** | supplements |
| :423 / :1852-1855 / :2460 | 「03:17 Narrative Event → Claude 出现」未描述触发机制 → **03:17 是确定性 Gate（A/B counter）**（见 二.7） | supplements |
| :2175 | §26.4 E2E「→ Character response → Claude appears」未规定如何触发 → **必须走 counter 路径（普通回合），不得玩家问 03:17**（D6） | supplements |
| :23 / :1031 / :2010 / :2478 | 多处断言「存档保存在 PostgreSQL」「刷新后仍能列出 PostgreSQL 中的存档」→ **dev/CI 实际走 JSON 存档仓库；Postgres 接线尚未成为已验证运行路径**（二.5） | supplements |
| :1492 | §21.2 列 12 个 checkpoint（含 AS_CH1_0317_TRIGGERED 等）→ **仅实现前 5 个**（`backend/app/save/checkpoint.py`） | supplements |
| :1578 | §23 未说明 Claude 出现之后调查主线在 Vue 的位置 → **无 Vue UI，推迟用 LingChat 选项功能实现（D4）** | supplements |
| :2093 / :210-216 / :2491 | 「AGPL notice完成」/「至少应包含 /LICENSE /NOTICE.md /THIRD_PARTY_LICENSES.md」未说明现状 → **LICENSE 缺失，Task 9 必须补**（二.9） | supplements |
| :41 / :54-56 | §1 把 `12 — LingChat UI 与多角色剧本系统借鉴落地方案.md` 列为必读且优先级最高 → **该文档已废弃（docs/abandon/12），已被 docs/13 取代** | stale |

### 3.4 docs/abandon/12-LingChat-UI与多角色剧本系统借鉴落地方案.md

| 行 | 现状 | 类别 |
|---|---|---|
| :3 | 文件头标「**文档状态：Active**」→ **已废弃（位于 docs/abandon/），被 docs/13 取代**（Script DSL 已在 `backend/app/script/` 实现） | stale |
| :5 | 「重新实现到现有 Next.js + FastAPI 架构中」→ **项目无 Next.js/React** | **contradicts** |
| :202 | 「冻结技术栈 Frontend: Next.js + React + TypeScript」「Vue Component → 机械翻译 React」→ **无 React；Vue 迁移到 Vue 3，从未翻译成 React** | **contradicts** |
| :1815 | 「1. React GameStage」交付物 → **无 React；对应层在 frontend-vue（Vue 3）** | **contradicts** |
| :1874 | 「不改变现有 Next.js + FastAPI 架构」→ **当前架构是 Vue 3 + Vite + FastAPI** | **contradicts** |

### 3.5 docs/abandon/01-03（第一章前身，ACCS 时代）

| 文件 | 现状 | 类别 |
|---|---|---|
| abandon/01-第一章玩法简述.md | 整篇为 ACCS 时代玩法（日志密钥→ACCS 审计日志→缓存漏洞→Request Patch→离开）→ **ACCS/门禁漏洞/缓存利用已废弃**（docs/09 §3.1），改为封闭空间调查 + 证词 + 私审 + Recovery | stale |
| abandon/02-ACCS实现需求.md | 定义 ACCS 门禁系统实现需求 → **ACCS 已废弃，无对应领域逻辑** | stale |
| abandon/03-第一章第一小节剧本.md | ACCS 时代剧本（日志密钥 114514、缓存 Bug、Request Patch、Exit Gate 逃脱）→ **与当前冻结方向不符，不得作为生产内容** | stale |

### 3.6 docs/MVP/

| 文件:行 | 现状 | 类别 |
|---|---|---|
| MVP/00 §11 (:245) | 「项目当前处于：轻量技术验证阶段」→ **TV-01~17 已全部 PASS，已进入 docs/13 落地实施（Task 0-8 完成）** | stale |
| MVP/01 :533 / :667 | 「不要求当前阶段实现完整正式存档系统」→ **完整存档系统已实现（Task 6-8，二.5）** | supplements |
| MVP/02 :120 | 「Vue 核心链路验收通过后**再删除**旧前端」→ **用户决策改为迁移（frontend-deprecated/），不删除**（D1） | supplements |
| MVP/06 :228 | 完成 Gate 只到 TV-16、以 TV-16 为 Final Gate → **TV-17（对话输出双模式 / Scripted Opening + ON_EVENT）已实现并 PASS**（validation-results/TV-17） | supplements |

### 3.7 第一章功能文档

| 文件:行 | 现状 | 类别 |
|---|---|---|
| docs/10-第一章调查内容配置.md :329 | STAGE 02「系统时间到达 03:17」将 03:17 触发描述为**时间触发** → **实际是确定性 Gate（A/B counter），非墙钟时间**（二.7） | supplements |
| docs/11-第一章调查主线落地说明.md :661 | §15 断言前端「至少需要」完整调查 UI（Hotspot/Evidence 出示/Contradiction/Inference/私审小游戏等）→ **frontend-vue 无此 UI，已推迟用 LingChat 选项功能实现（D4）**；该清单是「最终前端」目标，非当前能力 | supplements |
| docs/09-第一章开发落地备忘录.md :640 | §17「第一章最终前端需要」清单（Evidence/Contradiction/Inference/私审/Recovery/Security Review/DELETE/Bad End UI）→ **大部分在 frontend-vue 无实现，属推迟工作**（D4） | supplements |

---

## 四、后续动作建议

1. **恢复 Task 9**：读 plan 文件（`.claude/plans/serene-strolling-rainbow.md`，含 9 个 Step + Cutover 8 条件核对 + 验证命令），按 D1-D8 执行。
2. **修正文档**（docs-first，按任务范围逐步修）：docs/13 的 "React" 漂移 → 统一「旧前端 / vanilla 前端 / Vue Cutover」；§34/§35 Task 状态；docs/MVP/00 阶段描述；abandon/12 的 Active 头标；docs/10 §03:17 触发描述。**CLAUDE.md / AGENTS.md 暂不动**（前者在途修改，后者未在本次交接范围）。
3. **本交接完成后**：99 / 98 两份 TEMP 文档可随 Task 9 结束归档或删除。
