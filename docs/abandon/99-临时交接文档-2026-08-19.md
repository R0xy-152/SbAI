# 99 — 临时交接文档（2026-08-19）

> **状态：已归档（2026-08-19）。** docs/13 Task 9（视觉回归与 Vue Cutover）已完成并 PASS，本一次性交接文档随任务结束归档至 `docs/abandon/`。记录见 `validation-results/docs13-task9/result.md`。

> **状态：临时交接文档（TEMP）**。用途：Task 9 规划中途被叫停，本文供后续会话 / 上下文快速接续。阅读本文后请先读 `docs/98-最新决策与信息-2026-08-19.md`（决策与最新信息的权威补充），再读 docs/13 Task 9 相关章节。
> 日期：2026-08-19。本文为一次性交接，完成任务后可删除或归档。

## 1. 项目当前阶段

《完蛋，我被AI娘包围了》：AI-native Galgame / 对话式悬疑解谜游戏。**docs/13（LingChat 前端源码迁移、开始界面与存档系统落地方案）Task 0-8 已全部完成并 PASS，仅剩 Task 9（视觉回归与 Vue Cutover）**。

| Task | 内容 | 状态 |
|---|---|---|
| 0 | 建立迁移基线 | PASS_WITH_LIMITATION |
| 1 | 建立 Vue 3 Web Frontend | PASS_WITH_LIMITATION |
| 2 | 迁入 LingChat Standard Game UI | PASS |
| 3 | 去除 Tauri 与 LingChat Runtime 依赖 | PASS |
| 4 | 接入现有 FastAPI Game Runtime | PASS |
| 5 | 实现 Title Screen | PASS（commit d59591e） |
| 6 | 实现 Backend Save Snapshot | PASS（commit 63d5680） |
| 7 | 实现 Save / Load UI | PASS（commit d0cb382） |
| 8 | 接入 Auto Save | PASS（commit 71dc034，已 push） |
| 9 | 视觉回归与 Vue Cutover | **规划中（本次交接点）** |

## 2. Task 8（Auto Save）完成摘要

- 新增 `backend/app/save/checkpoint.py`：4 个稳定 checkpoint（AS_CH1_OPENING_COMPLETE / AS_CH1_CLAUDE_APPEARED / AS_CH1_INF01_CONFIRMED / AS_CH1_INF03_CONFIRMED + AS_CH1_RECOVERY_ENTRY），纯函数读 `NarrativeState`，捕获记录进 `narrative_flags` 随快照持久化（once-per-session）。
- Auto Save 是 Narrative commit 后的 **side effect**（§21.3：commit → checkpoint reached → capture，绝不保存后再更新），只写单一 AUTO slot。
- 修了一个生产接线 bug：`main.py` 原 `orchestrator.save_service = ...` 赋到不存在的公开属性 → 改 `orchestrator._save_service`。
- 修了 `test_save_api.py` 偶发失败：根因是 shell 带 `DEEPSEEK_API_KEY` 时走真实 DeepSeek → narrative interpreter 降级 noop → checkpoint 不触发；`_app()` 内 `monkeypatch.setenv("GAL_PROVIDER","mock")` 固定 fixture。
- 后端 369 passed / 12 skipped；前端 typecheck + build PASS；浏览器 E2E 验收全过（opening → AUTO、03:17 → Claude → AUTO updated_at 移动 + 快照携带 flag、Continue 恢复为新 Active Session）。

## 3. Task 9 规划状态（本次交接点）

**Plan 文件**：`C:\Users\QMCHEN\.claude\plans\serene-strolling-rainbow.md`（plan mode 下已写入完整方案，含 9 个 Step、Cutover 8 条件核对、验证命令、风险、明确不做）。该文件在 `.claude/plans/`，不在仓库内，不会随仓库 push。

**用户已确认的决策**：
1. 旧前端 `frontend/` → 移动为 `frontend-deprecated/`（不删除）。
2. 视觉回归用 **Playwright screenshot 基线**（入库，1366x768 + 1920x1080，6 个 snapshot：TITLE_EMPTY_SAVE / OPENING_DEEPSEEK_ONLY / CLAUDE_APPEARS_TWO_ROLE / LONG_DIALOGUE / SAVE_PANEL / LOAD_PANEL）。
3. **「第一章主线可运行」以 docs/13 §7.2 验收链路为准**（Title→New Game→Opening→Player Input→Response→03:17→Claude 出现→Save→Reload→Load→恢复）。
4. **线索调查通过调用 LingChat 的「选项功能」实现，这一块先搁置** —— 调查主线 UI（证据面板/推理/私审/收尾/其余 3 个 hotspot）推迟到后续任务，Task 9 不实现。

**ExitPlanMode 被拒后的 4 项修正（用户明确要求，执行 Task 9 必须遵守）**：
1. **Docker Compose 下核心资源 404 不能作为「已知限制」放过** → 必须修复并验证。根因：backend 容器 `REPO_ROOT`（静态挂载目录）指向容器根 `/`，镜像只含 `/app/backend`，且 nginx 只代理 `/api`。修复：backend Dockerfile COPY `char/` `backgroud/` `frontend-deprecated/` 进容器根；nginx.conf 加三棵资源路径的反代。本机 Docker 29.7.2 可用，需 `docker compose up --build` 实测。
2. **03:17 的视觉/E2E 不得用「玩家问 03:17」作为触发** → 必须走实际确定性 Gate：获得纸条（EV01）→ 满足规定对话轮数（`pre_0317_player_turns >= 2`）→ 03:17 自动发生。
3. **所有新记录（result.md / commit / 任务名）必须写「Vue Cutover」，不得写「React Cutover」。**
4. **旧前端是 vanilla HTML/CSS/JS，不是 React**（docs 里的 "React" 全是文档漂移）；新记录不得把旧前端错标为 React。

## 4. 交接必须知道的「坑」与约束

- **CLAUDE.md 有用户/编辑器侧在途修改，一律排除在提交之外**（`git add` 用显式路径，绝不用 `git add -A`）。
- **GAL_PROVIDER=mock 是测试 fixture 契约**（CLAUDE.md）：测试 / Playwright webServer 都必须固定 mock；shell 有 DEEPSEEK_API_KEY，不固定会走真实 DeepSeek → 网络抖动 → 测试偶发失败。
- **文档漂移**：docs/13 反复称「旧 React Frontend」，实际旧前端是 `frontend/`（vanilla HTML/CSS/JS），新前端是 `frontend-vue/`（Vue 3）。本项目**不存在 React**。
- **根目录 `LICENSE`（AGPL-3.0 全文）缺失**，Task 9 必须补（Cutover 条件「AGPL notice完成」）；`THIRD_PARTY_LICENSES.md` + `NOTICE.md` 已齐。
- **dev 与 Docker 的静态资源路径语义**：dev 下 backend `main.py` 静态挂载 = 仓库根（`REPO_ROOT = parents[2]`），`/char` `/backgroud` `/frontend` 可访问；Docker 下 REPO_ROOT = 容器根 `/`，目前这三者 404 —— 修复方案见 §3 修正 1。
- **`git mv frontend frontend-deprecated` 后**，Vue 两处硬编码路径必须跟着改：`frontend-vue/src/adapters/asset-resolver.ts`（claude/chatgpt/doubao）与 `frontend-vue/src/api/assets.ts`（ASSET_BASE），并给 `vite.config.ts` dev proxy 加 `/frontend-deprecated`；`.dockerignore` 需把失效的 `frontend/` 条目删掉、且**不得排除** `frontend-deprecated/` `char/` `backgroud/`。
- **frontend-vue 无任何测试设施**（无 vitest/playwright），需新增 devDependency（§26.1/§26.2 直接理由）。npm registry 直连可达；Playwright CDN 可达；备用代理 `HTTPS_PROXY=http://172.26.17.176:10808`。
- **前端仅负责表现**；剧情推进 / Fact / 状态变化 / Save Capture 全部由 Backend 权威决定。API Key 只在 Backend 环境。

## 5. 常用验证命令

```bash
cd /d/gal/backend && GAL_PROVIDER=mock .venv/Scripts/python -m pytest -q      # 后端全量（现 369 passed / 12 skipped）
cd /d/gal && for f in frontend/tests/*.cjs; do node "$f"; done                 # 旧前端 .cjs 测试
cd /d/gal/frontend-vue && npm run typecheck && npm run build                   # Vue typecheck + build
cd /d/gal/backend && DEEPSEEK_API_KEY=sk-xxx .venv/Scripts/python -m uvicorn app.main:app --port 8000   # 真实 DeepSeek 启动
```

## 6. 下一步（Task 9 完整执行路径）

1. 读 plan 文件 + `docs/98-最新决策与信息-2026-08-19.md`。
2. 按 plan 的 Step 1-9 执行：LICENSE 补全 → git mv + 资源路径/proxy/dockerignore → Docker Compose 资源路径修复并实测 → Vitest 组件测试（§26.1）→ backend §26.3 补 2 测试 → Playwright 视觉回归（§26.2）→ 新旧对比证据 → E2E（§26.4）→ result.md + commit `feat(docs13-task9): 视觉回归与 Vue Cutover` + push。
3. 完成后写 `validation-results/docs13-task9/result.md`，把本文标记为已完成并清理。

## 7. 本次交接未完成的事项

- Task 9 尚未实现任何代码（仍在规划，plan 已写好）。
- 尚未执行任何提交（除本文与决策文档外的代码改动为 0）。
- 未修改 CLAUDE.md（在途修改保持原样）。
