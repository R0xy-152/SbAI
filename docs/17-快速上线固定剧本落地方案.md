# 17 — 快速上线固定剧本落地方案（临时）

> **文档状态：Active（临时措施）**
> **性质：** 本方案是「优先部署上线、暂缓完整剧情」的临时措施，**取代正式第一章玩法成为现役入口**，但正式第一章系统（docs/10-14 的调查 / 推理 / 私审 / Recovery / 安检）的代码全部保留、只是入口隐藏，后续评审版剧本定稿后以正式实现替换本方案。

## 0. 用户决策（实施必须遵守）

| 决策点 | 结论 |
|---|---|
| AI 回复 | 故事模式无自由输入框（纯固定剧本推进）；**结局后「继续聊天」进入 /game 自由聊天，AI 回复由 DeepSeek 提供**（2026-08-21 用户决策） |
| 剧本来源 | docs/story/07-First-Chapter-Script-v2-DeepSeek-Rewrite.md（**评审阶段文稿，临时采用**，不作为正式真相源） |
| 内容范围 | 仅实现 07 中的**字面对白 + 3 个选项**（SC01 / SC03 / SC09 的 A/B/C）；缺台词的场景用系统叙述行概括过渡 |
| 推进方式 | 纯线性视觉小说：点「继续」逐行推进，选项点弹 A/B/C 窗口 |
| 结局 | SC14 播完后显示「第一章 完 / To Be Continued」画面；可返回标题，或「继续聊天」进入自由聊天（复用同一会话） |
| 旧调查玩法 | 代码保留；原「AI 对话玩法」改名为「序章」，迁入「开始游戏」章节下级菜单且为当前唯一解锁章节（2026-08-21 用户决策）；存档按类型路由（故事存档→/story，旧玩法/结局后存档→/game） |
| 音效 | 搁置（不实现） |
| 演出 | 场景系统接线（2026-08-21）：scene_id 驱动演出——SC05/SC14 入场 glitch 脉冲、SC14 叠加 shake、SC03 暗化光照（brightness 0.45）、场景切换淡入标题卡（标题取自剧本已有 scene title），全部只用现有素材 |
| 立绘 | 缺失立绘先用占位（豆包已有 placeholder.svg），用户后续补充后替换 |
| 存档 | 保留存档 / 读档 / 自动存档（场景边界 = 自动存档检查点） |
| 部署 | 服务器信息待用户提供；先本机 docker-compose 全量验证 |

## 1. 实现结构

### 后端（backend/app/script/）

- story_content.py —— 07 剧本内容数据：14 个场景、全部字面对白（含 Player 台词）、3 个选项点（每个选项携带专属回应台词）、缺台词场景的系统叙述行。**内容与 Runtime 分离**，载入时 fail closed 校验（坏 speaker / emotion / 空台词 / 嵌套选项 / 重复 id 一律拒绝启动）。
- story_runtime.py —— StoryRuntime：链表式节点图（台词节点带 next 指针；选项分支末句 next 跳过选项节点直指合并主线，分支台词不进主线推进序列），每会话整数游标，current / advance / choose / snapshot / restore。**不触碰 NarrativeState / Evidence / Investigation 等既有权威状态**，与既有 Script DSL（docs/12）并行、互不依赖。

### 后端接线

- PersistedSession 新增 story_cursor 字段（向后兼容：旧快照缺省 None = 故事未开始），随既有 Session / Save 快照链路持久化——刷新、读档、自动存档自然携带剧本进度。
- GameOrchestrator 新增 story_current / story_advance / story_choose：游标移动 → 台词写会话历史（History 面板可用）→ 持久化 → 场景边界自动存档。
- SaveSnapshotService.auto_save_story：故事模式场景边界 / 选项提交即 checkpoint，直接覆写 AUTO slot（故事模式没有 Narrative checkpoint 机）。
- app/api/story.py 三个端点：

| 端点 | 语义 |
|---|---|
| GET /api/story/current?session_id= | 刷新 / 读档恢复当前展示节点（不移动游标） |
| POST /api/story/advance {session_id?, player_id?} | 「继续」：下一节点；首次调用即开始故事 |
| POST /api/story/choose {session_id, option_id, player_id?} | 提交 A/B/C 选项，返回该选项第一句台词 |

### 前端（frontend-vue/）

- src/api/story.ts —— 三个端点的类型与封装。
- src/views/StoryView.vue —— 剧本播放器（新路由 /story）：无输入框，GameDialog 恒为 responding 态（「▼」= 继续），选项点弹 StoryChoiceWindow，结局显示 StoryEnding；复用系统菜单 / 存档 / 读档 / 历史 / 加载演出 / 睁眼转场。
- src/components/game/standard/StoryChoiceWindow.vue —— A/B/C 选项窗口（必须选择，无「继续对话」关闭项）。
- src/components/game/standard/StoryEnding.vue —— 「第一章 完 / To Be Continued」结局画面。
- 路由：/story 新增；Continue / LoadView 仍按存档类型恢复。Title「开始游戏」现先进入 `/chapters`，仅「序章」可进入旧 GameView（/game，AI 对话 + 调查玩法）；/story 不再是新开局入口。

## 2.5 结局后自由聊天与场景演出（2026-08-21 追加）

### 2.5.1 结局后自由聊天（AI 接入，不写剧情）

- **边界**：不新增任何剧情文字；结局后的对话是生成式 AI 回复（DeepSeek 角色 + 既有角色运行器），与旧 /game 玩法完全复用（/api/chat → handle_turn 全链路）。
- 后端：故事会话直接可进 /api/chat（实测 200；故事台词已按角色写入历史，DeepSeek 上下文自然延续）；`story_progress()` 暴露游标 + finished 供存档路由；`/api/saves/{id}/load` 响应新增 `story_cursor` / `story_finished`。
- 前端：StoryEnding 新增「继续聊天」→ /game（复用同一会话）；存档路由助手 `saveTargetRoute`：故事未完结 → /story，已完结 / 旧玩法 → /game；原标题画面「AI 对话玩法」改名为「序章」并迁入 `/chapters` 章节选择页（新开一局经典玩法）。
- AI 提供方：仅 DeepSeek（用户决策「只用 deepseek 做角色扮演」）；compose 新增 `DEEPSEEK_API_KEY` 透传，缺省空 = 回落 mock（线上无 key 也能玩，只是回复为确定性 mock）。

### 2.5.2 AI 玩法新开局：删除前置剧情 + 常驻背景图（2026-08-21 用户需求）

- **删除前置剧情**：「序章」（原「AI 对话玩法」）新开局不再调用 /api/chat/opening —— 不播开场白「你醒了，别怕」，也不自动弹出「选择行动」选项窗口，直接进入与 DeepSeek 的自由对话；会话由首个玩家消息经 /api/chat 创建（后端 mint session，响应带回 session_id）。旧调查玩法仍经左上角「行动」按钮正式可见（既有决策）。后端 SCRIPT_OPENING 节点与 /api/chat/opening 端点原样保留（旧前端与测试依赖），仅现役前端新开局路径不再消费。
- **常驻背景图**：序章（AI 对话玩法）常驻背景替换为用户提供的图 `backgroud/background_ai.png`（AI 生成教室场景）；`applyPresentationStateView` 背景参数化（默认仍为故事模式的 background1.png）。StoryView 故事模式背景不变。

### 2.5.3 场景演出接线（纯表现，不写剧情）

- `story_content.py` 新增 `SCENE_PRESENTATION`（场景演出配置，与台词内容分离）：SC03 暗化光照、SC05 glitch、SC14 glitch + shake；StoryRuntime 载入时 fail closed 校验（未知场景 / 未知效果 / 非法光照拒绝启动）。
- 后端视图新增 `scene`（scene_id / title / presentation），title 取自剧本已有 scene title。
- 前端新增 `ScreenEffects.vue`（SCREEN_GLITCH 扫描线+闪光+撕裂条；SCREEN_SHAKE 视图抖动；prefers-reduced-motion 停用）与 `SceneTitleCard.vue`（切换淡入淡出）；StoryView 消费 scene：光照常驻、入场效果脉冲播放（2.6s）、标题卡切换显示；GameView 同步消费（旧玩法 SCREEN_GLITCH 此前无渲染）。

## 2. 与既有系统的关系（边界）

- **旧调查玩法不删除**：/api/chat、/api/game/*、Script DSL、Evidence / Deduction / Private Interview / Recovery / Security Review 全部原样保留，后端测试全绿（426 passed）。
- **故事模式不写 Narrative State**：故事会话的 narrative 部分保持默认（phase=opening / scene=binding_room），与旧存档校验白名单兼容。
- **旧存档兼容**：读取旧调查玩法存档时 story_cursor 缺省 → 故事从第一个节点开始（有限制、不崩溃）。

## 3. 已知限制（快速上线接受）

1. 内容来自评审阶段文稿，后续以正式剧本替换 story_content.py 即可；
2. 无音效；差分立绘已接线（resolver 按 {角色}_{表情英文id}.png 探测、缺失回落 main），豆包仍为占位图（补图后自动生效）；
3. 缺台词场景为系统叙述行概括（新写占位文本）；
4. 07 中 SC02 的自由聊天段未在故事流程内实现（AI 停用），只保留两段推荐固定桥段；自由聊天以**结局后模式**提供（§2.5）；
5. 自动存档粒度 = 场景边界 + 选项提交（约 17 次 / 完整流程）；
6. 结局后自由聊天复用旧 /game 玩法（含调查选项）；AI 仅 DeepSeek（其他角色由既有 availability 门控 fail closed 拒绝）；服务器已配置 DEEPSEEK_API_KEY（2026-08-21 起，真实 AI 回复生效，实测 persona 回复正常），key 缺失时回落 mock。

## 4. 部署

- 复用 docker-compose.yml（frontend-vue nginx / backend / postgres，已加 restart: unless-stopped）；
- 环境变量经项目根 .env 注入（模板 .env.example）：FRONTEND_PORT（默认 8080，服务器可设 80）、GAL_PROVIDER（快速上线无 AI key，默认 auto 回落 mock）、GAL_CORS_ORIGINS、POSTGRES_PASSWORD；
- 完整步骤 / 验证清单 / 域名 HTTPS / 更新回滚 / 备份：见 **deploy/DEPLOY.md**；
- 服务器信息（IP / SSH / 域名 / 端口）待用户提供后执行远程部署。

## 6. 缺失美术素材清单（待用户补充）

> 现有素材：backgroud/ 只有 background1.png（绑定房间）与 background_title.png（标题）；char/ 立绘见下表。剧本实际使用的表情（story_content.py 统计）：DeepSeek = annoyed/embarrassed/happy/nervous/neutral/sad/serious/surprised；Claude = serious；豆包 = embarrassed；ChatGPT 无字面台词。

### 6.1 场景背景

| 场景（07） | 需要的背景 | 现状 |
|---|---|---|
| SC01 开场 | 黑屏 → 系统启动画面 | 黑屏可用纯色；启动画面用系统文字行呈现（非必需出图） |
| SC01–SC04 | 醒来房间（终端 + 桌面） | background1.png 暂代；如与正式设定不符需正式房间图 |
| SC03 | 房间**暗版**（主灯失效） | **缺失** |
| SC03 | 房间**亮版**（恢复照明） | **缺失**（建议同一场景两版亮度，淡入切换） |
| SC04 | 桌面纸条特写（拓印 V03） | **缺失**（当前系统叙述行） |
| SC05 | 03:17 断电 / Glitch | 可用现有 SCREEN_GLITCH 特效替代；故障画面为可选美术 |
| SC07 | 调查界面：Main Terminal / C-02 隔离门 / Character Registry | **缺失**（当前系统叙述行） |
| SC09 / SC13 | 记录对比界面（DEEPSEEK#03 / #04、PLAYER_V04） | **缺失**（当前系统叙述行） |
| SC14 | 警报 / SANDBOX INTEGRITY FAILURE | **缺失**（当前系统叙述行 + 可加 glitch） |

### 6.2 角色立绘

| 角色 | 已有素材 | 缺失 |
|---|---|---|
| DeepSeek | main + 全套 8 差分（annoyed / embarrassed / happy / jealous / nervous / sad / serious / surprised，2026-08-21 入库） | 无（07 用到的 8 表情全部覆盖，已接线） |
| Claude | main + angry / embarrassed / happy / jealous / nervous / serious / surprised（2026-08-21 入库） | 无（07 全程 serious 已覆盖；sad 无差分，若用到回落 main） |
| ChatGPT | main + annoyed / embarrassed / happy / jealous / nervous / serious / surprised（2026-08-21 入库） | 无（07 无字面台词，差分供旧玩法 /game 使用） |
| 豆包 | 仅 placeholder.svg | **全部缺失**：至少主立绘 + embarrassed（07 有 1 句 embarrassed 台词，暂用占位图） |

### 6.3 代码侧待办（素材到位后执行）

- ✅ 已完成（2026-08-21）：asset-resolver.ts 接入 emotion → 差分文件映射（HEAD 探测 + 缓存，404/网络失败回落 main 单图，组件不感知资源细节）；豆包补图后自动生效，无需改代码；
- 素材文件名维持 {角色}_{emotion英文id}.png 约定（原中文名 deepseek_开心 / claude_shengqi 已统一为 happy / angry）。

## 5. 回滚

恢复旧玩法 = 把 Title / LoadView 的三个 router.push('/story') 改回 '/game'，并移除 /story 路由即可；后端 story 组件可整体保留或删除，不影响旧链路。
