# Python backend
backend/.venv/
backend/.env
__pycache__/
*.pyc
.pytest_cache/
# TV-14: runtime session snapshots (Session Restore data) — never committed
backend/data/

# Node
node_modules/
frontend-vue/dist/

# LingChat 参考源码（AGPL 原始文件，仅作迁移参考，不入库）
.lingchat-src/

# Claude Code worktrees (isolated checkouts) — never committed
.claude/worktrees/

# Deployment transfer bundle (repo snapshot for server sync) - never commit
gal-new.bundle

# macOS Finder metadata
.DS_Store

# 邀请码台账 / 二维码 / 导出产物（明文 Secret 与玩家留言，不入库，docs/20）
invite-codes.md
deploy/invite-qr/
developer-notes.xlsx

# 版权素材：本地演示用，不上传/不部署（kei 特写视频 / Aira OST 音频 / 视频首帧）
backgroud/kei_opening*.mp4
backgroud/kei_opening_poster.png
backgroud/aira_full.m4a
